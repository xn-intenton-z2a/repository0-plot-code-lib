# README_CLI_DOCS

Overview

Document CLI usage examples and explain the PNG rendering approach and any optional dependencies.

Specification

- Update README.md with example commands showing expression sampling and CSV plotting using plain command lines
- Explain how PNG output is produced and list installation steps for optional dependencies used for PNG rendering

Acceptance Criteria

1. README contains a CLI usage section with example invocations for expression sampling to SVG and CSV to PNG
2. README documents that PNG rendering requires an optional native dependency and gives exact installation guidance (for example, install sharp or node-canvas)

Implementation Notes

- Avoid heavy prose; include clear, copy-paste friendly example commands and a short note about how to enable PNG support when needed.