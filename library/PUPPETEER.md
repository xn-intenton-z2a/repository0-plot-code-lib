PUPPETEER (DEPRECATED)

This document has been deprecated and removed from SOURCES.md. The repository now prefers Playwright Test for browser-based assertions and end-to-end checks.

See library/PLAYWRIGHT.md for the extracted, actionable API usage and example patterns for asserting SVG content, attributes and element counts.

Original Puppeteer notes were removed from SOURCES.md to reduce duplication and align tests with the project's devDependencies (Playwright).
